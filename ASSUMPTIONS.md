# V3 运单全流程管理系统 - 需求理解与假设说明

## 概述

本文档针对考试题目中故意留白的业务规则，逐一说明假设、设定依据和设计理由。每项均给出具体数值或规则。

---

## ① 分级审批金额阈值

**设定：一级审批 ≤ 5000元，二级审批 > 5000元**

**依据：**
- 参考物流行业常见的货损赔付标准，单件货物价值通常在 100-5000 元区间
- 5000 元以下为常见小额理赔，可由一级审批人快速处理
- 5000 元以上涉及较大金额，需二级审批把关降低风险
- 阈值通过 `approval_config` 表可配置（`level2_threshold` 字段），后台可随时调整，不硬编码

---

## ② 审批超时时长

**设定：**
- 待审批超时：24 小时（超时后自动升级到二级审批）
- 一级审批超时：48 小时（超时后自动升级到二级审批）
- 二级审批超时：72 小时（超时后自动驳回）

**依据：**
- 24 小时（1个工作日）无人处理的工单应尽快升级，避免积压
- 48 小时（2个工作日）给一级审批人足够时间评估，同时不让上报人等太久
- 72 小时（3个工作日）作为二级审批的最后时限，超时自动驳回避免无限卡死
- 审批超时时长通过 `approval_config` 表可配置

---

## ③ 重提次数上限

**设定：被拒绝后最多重新提交 3 次，超过后自动关闭**

**依据：**
- 3 次重提给上报人足够的机会修正问题或补充信息
- 超过 3 次说明问题可能是根本性的（如运单信息本身错误），不应无限循环
- 自动关闭而非强制升级，因为连续被拒说明该工单可能不成立
- `max_reject_count` 通过配置表可调整

---

## ④ 物流异常类型 → 下游执行动作映射

| 异常类型 | 执行动作 | 是否赔付 | 赔付方向 | 库存影响 | 说明 |
|---|---|---|---|---|---|
| 丢件 (lost) | 理赔 + 重新发货 | 是 | 赔付客户 | 扣减库存 | 货物丢失，需赔付客户并补发 |
| 破损 (damaged) | 理赔 + 退货入库 | 是 | 赔付客户 | 增加库存 | 货物损坏退回，赔付客户 |
| 客户拒收 (rejected) | 退货入库 | 否 | - | 增加库存 | 客户拒收退回，一般不涉及赔付 |
| 超时未签收 (timeout) | 重新发货 | 否 | - | 扣减库存 | 超时退回，重新安排派送 |
| 地址错误 (address_error) | 重新发货 | 否 | - | 扣减库存 | 更正地址后重新发货 |

**品控类异常默认映射：**

| 异常类型 | 执行动作 | 赔付方向 | 说明 |
|---|---|---|---|
| 数量不符 (qc_quantity) | 退回供应商 + 重新采购 | 向供应商追偿 | 来货数量差异 |
| 外观破损 (qc_appearance) | 退回供应商 + 重新采购 | 向供应商追偿 | 来货质量问题 |
| 规格不符 (qc_spec) | 退回供应商 + 重新采购 | 向供应商追偿 | 规格与订单不符 |
| 标签错误 (qc_label) | 退回供应商 | 向供应商追偿 | 标签信息错误 |
| 批次异常 (qc_batch) | 退回供应商 + 重新采购 | 向供应商追偿 | 批次信息异常 |

---

## ⑤ 角色权限划分

| 角色 | 扫描录入 | 异常上报 | 一级审批 | 二级审批 | 快速放行 | 规则配置 | 查看工单 |
|---|---|---|---|---|---|---|---|
| 操作员 (operator) | ✅ | ✅ | ❌ | ❌ | ❌ | ❌ | 仅自己的 |
| 品控主管 (qc_supervisor) | ✅ | ✅ | ❌ | ❌ | ✅ | ✅ | 全部 |
| 一级审批人 (level1_approver) | ❌ | ❌ | ✅ | ❌ | ❌ | ❌ | 权限范围内 |
| 二级审批人 (level2_approver) | ❌ | ❌ | ✅ | ✅ | ❌ | ❌ | 权限范围内 |
| 管理员 (admin) | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | 全部 |

**关键权限规则：**
- 上报人不能审批自己提交的工单（后端强制校验）
- 品控主管可与审批人角色重叠，但操作受限制
- 品控主管的快速放行仅适用于品控异常工单
- 非对应层级的审批人不能越权操作

---

## ⑥ V2 数据同步频率与一致性策略

**策略：按需实时拉取 + 本地缓存**

**选择依据：**
- **异常上报时**：实时调用 V2 接口获取最新运单信息并刷新本地快照，确保关键操作的数据新鲜度
- **列表展示时**：使用本地快照表（1 小时有效期），减少对 V2 的请求压力
- **V2 不可用时**：降级使用过期缓存，前端明确标注"数据来源：本地缓存，同步于 XX 时间"
- **一致性边界处理**：若 V2 运单信息在处理期间发生变更（如金额更正），V3 在审批时以 V3 发起时的快照为准，差异通过接口同步日志记录，支持人工对比介入

**为什么不选定时批量同步：**
- 异常工单的创建频率相对较低（非高并发场景）
- 实时拉取能保证上报时校验的数据是最新的
- 定时同步存在窗口期延迟风险

---

## ⑦ 品控暂扣超时时长

**设定：品控暂扣超时 2 小时（独立于审批超时 48 小时）**

**为什么独立于审批超时：**
- 审批超时针对的是"人工审批"环节（48-72 小时）
- 品控暂扣超时针对的是"货物压仓"场景
- 货物在仓库中暂扣每多一小时就产生额外的仓储运营成本
- 2 小时足够品控主管完成复核，超时后自动升级到二级审批避免无限期占仓

**确定依据：**
- 参考仓库运营成本：日均仓储费约 ¥0.5-2/立方米，大量货物暂扣 48 小时成本显著
- 2 小时覆盖了一个班次内的复核窗口
- 与审批超时形成合理的速度梯度：暂扣(2h) < 待审批(24h) < 一级审批(48h) < 二级审批(72h)

**超时处理方式：**强制升级二级审批（自动创建升级记录，来源标记为 `auto_timeout`）

---

## ⑧ 品控规则触发阈值

**规则设计（全部可配置，不硬编码）：**

| 规则名 | 触发条件 | 阈值 | 严重度 | 审批层级 | 设计依据 |
|---|---|---|---|---|---|
| 数量差异检测 | quantity_diff_percent > 5% | 5% | high | 一级 | 5%为行业常见容差，超过需关注 |
| 严重数量差异 | quantity_diff_percent > 20% | 20% | critical | 二级 | 超过1/5差异说明来货异常严重 |
| 外观破损-轻微 | damage_level ≥ 1 | 1级 | low | 一级 | 轻微破损可以修复或降级处理 |
| 外观破损-严重 | damage_level ≥ 3 | 3级 | critical | 二级 | 严重破损只能退回供应商 |
| 规格偏差检测 | spec_deviation > 0 | 有偏差 | medium | 一级 | 规格不符影响使用 |
| 标签错误检测 | label_match = false | 不匹配 | medium | 一级 | 标签错误影响识别和追溯 |
| 批次异常检测 | batch_valid = false | 无效 | high | 二级 | 批次异常可能是系统性风险 |

**实现方式：** 所有规则存储在 `qc_rules` 表，通过后台管理页面可随时增删改查，无需修改代码

---

## ⑨ 品控主管角色权限边界

**品控主管可以做什么：**
- 扫描录入和异常上报（与操作员相同）
- **误判快速放行**：品控主管认定扫描结果为误判时，可绕过完整审批流程直接解锁批次、关闭工单
  - 操作需填写复核原因（必填，留痕记录）
  - 不允许静默放行
- 品控规则管理：增删改查品控触发规则
- 查看全部工单

**品控主管不能做什么：**
- 不能做审批操作（一级/二级审批是审批人的专属职责，防止权力过度集中）
- 不能越权覆盖审批人的审批结果
- 快速放行仅适用于品控异常工单（来源=扫描触发），不适用于物流异常工单

**角色重叠策略：**
- 一个人可以同时担任品控主管和审批人
- 但不能审批自己上报的工单（后端校验 `reporter !== approver`）
- 不能对自己操作扫描产生的工单做快速放行

---

## 主动澄清问题清单（如果可以向产品经理提问）

1. **发货后物流异常**：丢件/破损的理赔标准是按货值全额赔付还是按比例？谁来确定赔付金额？
2. **多租户场景**：如果涉及多商户/多仓库，工单归属和审批人权限的租户隔离粒度是什么？
3. **审批人离职兜底**：审批人离职后，已分配给他的待审批工单转交给谁？是否有默认备选审批人？
4. **赔付实际执行**：赔付记录生成后，财务对账和实际付款流程是否需要对接？还是仅做记录？
5. **品控规则的维护**：品控规则的阈值是否需要版本管理？规则变更是否需要审批流程？
6. **V2 数据变更通知**：V2 运单数据变更时，是否需要主动推送通知 V3，还是 V3 被动拉取即可？
7. **并发生成工单**：如果两个操作员同时对同一运单的不同 SKU 做扫描并都触发异常，是合并为一个工单还是创建两个独立工单？
8. **执行动作的可选择性**：审批通过后的执行动作（如退货 vs 重新采购）是由系统自动决定还是审批人可以选择？

---

## 单租户假设说明

当前 V2 数据不区分租户，V3 采用**单租户假设**。如需支持多租户：

**表结构调整：**
- 所有 V3 表新增 `tenant_id` 字段
- 审批人查询增加 `WHERE tenant_id = current_tenant_id` 条件
- 扫描录入时校验 `SKU + 运单` 属于当前租户
- 快照表同步时增加租户隔离

**查询逻辑调整：**
- 工单列表按租户过滤，操作员只能看到自己租户的运单
- 审批人列表只能看到自己权限范围内的工单（租户 + 角色双重过滤）

---

## 老系统二开意识

**如果 V2 原本没有现成的对外接口：**

1. **新增接口不破坏现有调用方**：使用 `/api/v2/external/` 作为独立路径前缀，与 V2 现有业务接口物理隔离
2. **接口版本策略**：URL 路径不包含版本号（如 `/api/v2/external/waybills`），版本信息通过响应头 `X-API-Version` 传递
3. **字段向后兼容**：新增字段使用可选（`?`），不删除已有字段；字段类型变更使用新增字段（如 `amount_decimal`）而非修改原字段
4. **灰度上线**：新增接口先在 V3 内部验证，确认稳定后再对外暴露

**V2 接口字段升级时 V3 如何应对：**
- V3 快照表使用 JSONB 存储原始数据，字段变更不影响已有快照
- 新的同步操作会刷新快照，自然包含新字段
- 对于关键字段（如金额），V3 同时存储 `total_amount`（解析后的数值），不受 V2 字段类型变更影响
- 接口同步日志记录每次调用的响应摘要，可追踪不一致

---

## 反思题（考点9）

### 1. 如果题目直接给出了精确数值，架构设计有什么不同？

如果题目直接给出了精确数值（如"5000元"、"48小时"），我会将这些值写在代码常量中，而不会设计 `approval_config` 配置表。但现实中产品需求经常变化，可配置的架构能更快响应业务调整。本题的"留白"恰好验证了可配置架构的价值——数值不是重点，架构的灵活性和工程师的判断力才是。

### 2. "审批状态变更"与"库存/赔付联动"之间一致性的最大风险点？

最大的风险点是"审批通过更新了工单状态为executing，但库存变更或赔付记录写入失败"这种部分成功状态（中间态）。如果不用数据库事务，我会采用"最终一致性+补偿任务"方案：
- 审批通过后先标记工单状态为 `executing`
- 异步执行库存变更和赔付记录写入
- 每个操作有独立的成功标记
- 定时补偿任务扫描 `executing` 状态的工单，检查下游联动是否完成
- 未完成的自动重试，多次失败后标记为异常并告警

### 3. 面对模糊需求，"先问清楚"还是"先做版本"？

我倾向于"先做出一个版本再迭代"。理由是：① 一个可运行的版本能帮助产品经理更直观地理解自己的需求，比纯靠语言沟通效率高；② 模糊需求往往是因为产品经理也无法完全预见所有细节，运行版本能暴露隐藏问题；③ 技术可行性验证需要实际代码。但关键前提是：第一版要把可配置的点做好，让后续调整不需要大面积重构。

### 4. 新增"客户投诉服务态度"类型需要改哪些地方？

改动成本不大：
- 在 `ExceptionType` 类型中新增 `"complaint"` 值
- 在 `EXCEPTION_TYPE_LABELS` 和 UI 下拉菜单中添加选项
- 在 `DEFAULT_CONFIG.exceptionActionMapping` 中添加映射（该类型不涉及库存联动，`inventoryImpact: null`）
- 表结构不需要改（`exception_type` 字段是 TEXT 类型，不限制值）

### 5. 工单量从 200 条增长到 20 万条，最先撑不住的是？

**最先撑不住的是列表查询的全表扫描**。20 万条数据，不带索引的列表查询每次都要扫描全表。处理方案：
- 确保 `status`、`exception_type`、`created_at` 列有索引
- 列表分页使用游标分页（`WHERE created_at < last_cursor`）替代 OFFSET
- 统计类查询（各种状态的计数）单独建物化视图或缓存
- 定时任务使用增量扫描（只处理 `updated_at` 在最近窗口内的工单）

### 6. 给 V2 新增对外接口时如何保证不破坏现有调用方？

**隔离策略**：
- 路径隔离：新接口使用独立路径前缀（`/api/v2/external/`）
- 响应结构：使用标准 `{ success, data, error }` 格式，不直接返回数据库模型
- 字段策略：只暴露必要字段，内部字段不对外

**V2 金额字段 int → decimal 变更时的兼容：**
- V3 快照表的 `total_amount` 使用 DECIMAL 类型，不受 V2 字段类型变更影响
- JSONB `raw_data` 存储原始响应，可对比差异
- 新增 `data_version` 标记快照版本，便于追踪
- 如果需要回填历史数据，通过接口同步日志可定位具体时间窗口
